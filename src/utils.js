const ENV = process.env
const tokenText = ["amazon authentication", "security alert", "amazon password assistance"]
const print = (...msg) => console.log(...msg)

module.exports = { ENV, print, tokenText }
